class UtilityService {
  // Fungsi Future umum
/*  Future<T> fetchData<T>(String url) async {
    // Misalnya, menggunakan HTTP GET request
    try {
      // ...logic fetch data, parsing, dll.
      // Return hasil sesuai tipe data yang diminta
      return result as T;
    } catch (e) {
      throw Exception("Failed to fetch data: $e");
    }
  }*/

  // Future<String> fetchData(String url) async {
  //   // Misalnya fetch data dari API
  //   await Future.delayed(Duration(seconds: 2)); // Simulasi delay
  //   return "Data from $url";
  // }
}
